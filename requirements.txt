# Laura Image Studio - Core Requirements
# Version: 2.0.0
#
# These are the ONLY packages installed by default.
# torch, torchvision, torchaudio are provided by ComfyUI itself.
# Optional feature dependencies are in requirements-optional-*.txt files.

packaging>=23.0
psutil>=5.9.0
requests>=2.31.0
pydantic>=2.0.0
huggingface_hub>=0.20.0
colorama>=0.4.6
tqdm>=4.66.0
