# Laura Image Studio - Optional Face Dependencies
# Install: pip install -r requirements-optional-face.txt
# Required for: face swap, face detection, LivePortrait

insightface>=0.7.3
filterpy>=1.4.5
scipy>=1.11.0
onnxruntime-gpu>=1.17.0; platform_machine == 'x86_64' and sys_platform != 'darwin'
onnxruntime>=1.17.0; platform_machine != 'x86_64' or sys_platform == 'darwin'
